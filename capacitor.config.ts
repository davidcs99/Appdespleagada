import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Calculadora apk',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
